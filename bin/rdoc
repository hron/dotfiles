#! /bin/sh

# This is a wrapper over rdoc and it forwards all the parameters to
# it. Manual usage would require specifying Hanna as a template when
# invoking RDoc on the command-line:

/var/lib/gems/1.8/bin/rdoc -o doc --inline-source -T hanna lib/*.rb
